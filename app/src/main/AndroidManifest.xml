<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pristinetravel.pristinetravels">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:largeHeap="true">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".application_page" />
        <activity
            android:name=".Contact_about_us"
            android:label="@string/title_activity_contact_about_us"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Custom_Itinerary"
            android:label="@string/title_activity_custom__itinerary"
            android:theme="@style/AppTheme" />
        <activity android:name=".Packages_activity"></activity>
    </application>

</manifest>